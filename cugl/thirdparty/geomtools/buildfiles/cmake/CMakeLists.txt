# PROJECT CMAKE FILE
# This file is autogenerated by the creator script
cmake_minimum_required(VERSION 3.10)

# set the project name and version
project(geomtools VERSION 1.0)

set(PCKG_DIR  "${PROJECT_SOURCE_DIR}/../..")
list(APPEND EXTRA_INCLUDES "${PCKG_DIR}/include")

# poly2tri
add_subdirectory(poly2tri)
list(APPEND EXTRA_LIBS poly2tri)

# clipper
add_subdirectory(clipper)
list(APPEND EXTRA_LIBS polyclipping)

add_library(geomtools INTERFACE)
target_link_libraries(geomtools INTERFACE ${EXTRA_LIBS})
