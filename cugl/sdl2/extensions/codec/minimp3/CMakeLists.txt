# PROJECT CMAKE FILE
# This file is autogenerated by the creator script
cmake_minimum_required(VERSION 3.10)

# set the project name and version
project(minimp3 VERSION 1.0)

# Get the directories
set(SDL2_PCKG_DIR  "${PROJECT_SOURCE_DIR}/../../..")

# Add the includes
list(APPEND EXTRA_INCLUDES "${PROJECT_SOURCE_DIR}")
list(APPEND EXTRA_INCLUDES "${SDL2_PCKG_DIR}/include")

# Compile the interface (we only support static for now)
set(MP3_SOURCES
    mp3stream.c
)

if (MSVC)
  add_compile_options(/wd4244)
  add_compile_options(/wd4267)
endif()

add_library(minimp3 ${MP3_SOURCES})
add_library(minimp3::minimp3-static ALIAS minimp3)
target_include_directories(minimp3 PUBLIC
                           "${PROJECT_BINARY_DIR}"
                            ${EXTRA_INCLUDES}
                           )
